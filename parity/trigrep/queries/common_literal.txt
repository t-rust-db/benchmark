# Present in most Rust files in any real tree: the floor, by design, not a
# failure -- a trigram index narrows nothing when the term is everywhere,
# so this measures the same full scan ripgrep always does.
fn 
