# A real, verified single-hit literal (2026-09-09, in the default corpus,
# ../../../sqlite-rs, scoped to exactly what `git ls-files --others
# --cached --exclude-standard` reports -- trigrep's own view): defined
# in exactly one file, tests/corpus/crash_torture_test.rs. Re-verify if
# the tree has moved on -- the query still runs either way, it just
# stops demonstrating a single-file narrow.
recover_hot_journal
